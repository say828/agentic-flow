{
  "schema_version": 1,
  "name": "example-service",
  "spec": {
    "planning_paths": [
      "sdd/01_planning",
      "sdd/02_plan"
    ],
    "canonical_targets_dir": "sdd/02_plan/canonical-targets"
  },
  "commands": {
    "build": "auto",
    "proof": "auto",
    "deploy_dev": "auto",
    "verify_dev": "auto"
  },
  "artifacts": {
    "proof_output": "tmp/proof-results.json"
  },
  "integrations": {
    "github": {
      "project_title": "Agentic Dev",
      "issue_label": "agentic-dev"
    },
    "discord": {
      "webhook_env_var": "DISCORD_WEBHOOK_URL"
    },
    "sdd": {
      "plan_dir": "sdd/02_plan",
      "verify_dir": "sdd/03_verify"
    }
  },
  "notes": [
    "This file is repo-local and keeps the plugin general.",
    "Use auto to let agentic-dev infer common repo commands, or replace any command with an explicit repo-local shell command.",
    "Point proof to a strict deterministic gate implemented inside the target repo when the auto-detected command is not sufficient."
  ]
}
