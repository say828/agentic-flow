{
  "name": "agentic-dev-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf node_modules",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
